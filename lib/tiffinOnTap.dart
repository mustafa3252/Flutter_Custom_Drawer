import 'package:flutter/material.dart';
import './tiffinList.dart';

class TiffinOnTap extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Column(
      children: _member.map{(){

      }}
    );
  }
}
